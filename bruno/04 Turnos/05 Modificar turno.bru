meta {
  name: Modificar turno
  type: http
  seq: 5
}

put {
  url: {{baseUrl}}/api/turnos/{{turnoId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

body:json {
  {
    "servicioId": "{{servicioId}}",
    "fechaHora": "2026-12-10T16:00:00-03:00"
  }
}
